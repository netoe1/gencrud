#include <iostream>
#include <stdlib.h>
#include <string>
#include <locale>
#include "../marg/marg.hpp"



int main(int argc,char** argv){
    ProgramArgs program_args(argc,argv);


    if(program_args.findAnArgument("create") != "undefined" && program_args.argv.at(2) == "create"){
        if(program_args.findAnArgument("create") != "undefined" && program_args.argv.at(2) == "create"){
        
    }
    }
}